# Prompts captured for this commit
_source: Codex conversation (manual recovery because the hook only indexes Claude Code transcripts)_
_captured: 2026-07-12T05:54:33+06:00_
_scope: user prompt for this deployment-data commit_

## 2026-07-12

The data/ folder (synthetic datasets used by the analytics engine) is
gitignored, but the deployed app on Vercel needs it at runtime — it's
not being regenerated during build. Fix this:

1. Remove the /data/ line from .gitignore
2. Run npm run generate locally to ensure data/ is fresh and complete
3. Commit the entire data/ folder to the repo
4. Confirm .gitignore change and data/ addition are both staged
5. Commit with an accurate PROMPT_SUMMARY, same pattern as before, and
   push to main

After pushing, tell me to trigger a redeploy on Vercel.
